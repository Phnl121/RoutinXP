# CLAUDE.md

As regras deste projeto valem para todas as ferramentas e estão em **[AGENTS.md](AGENTS.md)**. Leia esse arquivo antes de qualquer tarefa.

Em resumo:

- **Comece** lendo `PROGRESS.md` e `DESIGN.md`, e confira `git status` e `git log`.
- **Termine** com lint, build e um commit por ajuste. Sem push: quem publica é o usuário.
- Cada etapa visual passa pelo Impeccable antes e depois.
